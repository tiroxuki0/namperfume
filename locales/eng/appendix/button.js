import { values } from "../values"

export const button = {
  actions: values.appendix_buttons_0,
  create: values.appendix_buttons_1,
  filter: values.appendix_buttons_2,
  search: values.appendix_buttons_3
}
